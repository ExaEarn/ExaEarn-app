[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "exaearn"
version = "1.0.0"
description = "Official ExaEarn Python SDK"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
