{
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "turbo run build --filter=@exaearn/website",
  "outputDirectory": "apps/website/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
