# ExaEarn Phase 15A Token Migrations

Token migration records support:

- token rebrand
- contract migration
- network migration
- token swap

Contract migration requires explicit old and new contract details and rejects silent same-contract replacement. Migration approval and execution remain controlled operations, not automatic source-code changes.

