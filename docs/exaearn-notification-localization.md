# ExaEarn Notification Localization

The notification schema supports locale-specific template records and `NOTIFICATION_DEFAULT_LOCALE`.

Current default locale is English. Additional locales should be added as reviewed template versions, not free-form per-controller strings, so compliance/security copy remains consistent.
