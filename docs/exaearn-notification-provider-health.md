# ExaEarn Notification Provider Health

Provider health is modeled in `notification_provider_health` with states:

- HEALTHY
- DEGRADED
- DOWN
- UNKNOWN

This lets operations distinguish ExaEarn product health from email or push-provider availability.
